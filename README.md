# sequence-detector
**Sequence Detector:** A sequence detector is a digital circuit that **detects a specific pattern or sequence of bits** in an input stream. When the desired sequence is found, it produces an **output signal**.
